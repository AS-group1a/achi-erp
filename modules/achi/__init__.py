"""ACHI Scaffolding — company-specific extensions to OpenConstructionERP.

This is ACHI's own code. It is the OCE equivalent of `erp_next_custom`: a
first-class module the loader discovers on the filesystem, so nothing upstream
is patched and `OCE_VERSION` still upgrades cleanly.
"""
